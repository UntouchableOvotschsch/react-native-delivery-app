import React from "react";
import { connect } from "react-redux";
import MainScreenVolonteer from "./MainScreenVolonteer";

const OrderDetailContainer = connect()(MainScreenVolonteer)

export default MainScreenVolonteer